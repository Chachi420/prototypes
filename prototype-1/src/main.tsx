import React from 'react';
import ReactDOM from 'react-dom/client';
import BacklogGroomingAgent from './components/BacklogGroomingAgent';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BacklogGroomingAgent />
  </React.StrictMode>
);
